const assert = require('assert')
const error = require('../../../middleware/error')

describe('testing error handling middleware', ()=> {

    it('should test wether state is set to 404 and error name is set to CastError ', () => {
        
    })
 
})